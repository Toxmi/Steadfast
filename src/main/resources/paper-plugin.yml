name: Steadfast
description: $description
version: '${version}'

main: com.toxmi.steadfast.Steadfast
api-version: '1.21.11'
load: STARTUP
folia-supported: true

dependencies:
  - name: floodgate
    required: false

authors: [ Toxmi ]
